{% extends 'base.html.twig' %}

{% block title %}Page d'édition Admi - Calculate{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col mt-4">
                <h2>Modification des droits</h2>
                <div class="secondary-bg p-4">
                    {{ form_start(form) }}
                    <div>
                        <h3>{{ user.lastname }} {{ user.firstname }}</h3>
                    </div>
                    {{ form_row(form.roles) }}
                    <div class="d-flex justify-content-between">
                        <button type="submit" class="btn btn-primary">Enregistrer</button>
                        <a href="{{ path('app_admin') }}" class="btn btn-secondary">Retour</a>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
